# SUPERSEDED by hyprland.lua (2026-08-09). No longer installed or loaded --
# Hyprland 0.56 warns on the .conf format and 0.57 drops it. Kept only as a
# rollback reference; edit hyprland.lua instead.
#
# Hyprland Config for QuickShell Greeter
# Was copied to: /etc/greetd/hyprland.conf

# === Performance ===
env = GTK_USE_PORTAL,0
env = GDK_DEBUG,no-portals

# === Rendering Quality ===
env = GDK_SCALE,1
env = GDK_DPI_SCALE,1
env = XCURSOR_SIZE,24

# === NVIDIA Settings ===
env = LIBVA_DRIVER_NAME,nvidia
env = GBM_BACKEND,nvidia-drm
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = __GL_GSYNC_ALLOWED,1
env = __GL_VRR_ALLOWED,0

# === Monitor Configuration - BOTH ENABLED ===
monitor = DP-2,2560x1440@165,0x0,1
monitor = DP-1,2560x1440@165,-1440x-560,1,transform,1

# === Cursor (NVIDIA) ===
cursor {
    no_hardware_cursors = true
}

# === Appearance ===
general {
    gaps_in = 0
    gaps_out = 0
    border_size = 0
}

decoration {
    rounding = 0
}

animations {
    enabled = false
}

misc {
    disable_hyprland_logo = true
    disable_splash_rendering = true
    force_default_wallpaper = 0
}

workspace = 1, monitor:DP-2, default:true
workspace = 2, monitor:DP-2

# === Launch QuickShell Greeter ===
exec-once = /etc/greetd/launch.sh; hyprctl dispatch exit
