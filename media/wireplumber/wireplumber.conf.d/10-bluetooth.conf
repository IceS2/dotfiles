-- Bluetooth codec configuration

monitor.bluez.properties = {
  -- Enable all high-quality codecs
  ["bluez5.codecs"] = "[ sbc sbc_xq aac ldac aptx aptx_hd ]",

  -- LDAC quality (hq = highest quality, 990kbps)
  ["bluez5.a2dp.ldac.quality"] = "hq",

  -- Enable hardware volume control
  ["bluez5.enable-hw-volume"] = true,

  -- Enable SBC-XQ (enhanced SBC)
  ["bluez5.enable-sbc-xq"] = true,

  -- Auto-connect profiles on startup
  ["bluez5.auto-connect"] = "[ a2dp_sink hfp_hf hsp_hs ]",
}

-- Device-specific rules (high priority for Bluetooth)
bluez_monitor.rules = {
  {
    matches = {
      {
        { "node.name", "matches", "bluez_output.*" },
      },
    },
    apply_properties = {
      ["priority.session"] = 9500,  -- Higher than wired devices
      ["node.pause-on-idle"] = false,  -- Don't auto-suspend Bluetooth
    },
  },
}
